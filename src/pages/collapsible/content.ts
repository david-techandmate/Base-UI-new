/** Sample copy shared by the Collapsible demos. */

export const SUMMARY =
  'Collapsible is the single-panel primitive that Accordion is built from. One trigger, one panel, a boolean open state.';

export const PARAGRAPHS = [
  'Base UI measures the panel and publishes its natural height as --collapsible-panel-height, which is what makes a height transition possible without a hard-coded value.',
  'Because the panel is a plain div, it can hold any layout — prose, a form, a table, or another collapsible.',
];
